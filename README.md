# 🛡️ LOGIC PROTOCOL V3

Logic Protocol V3 is an AI-powered text analysis system utilizing a **Flask** backend and a **Qwen2.5-coder** local LLM via **Ollama**.

## 🚀 System Requirements
- **Python 3.10+**
- **Ollama** (with `qwen2.5-coder` model installed)
- **Port 3001** (must be available)

## 🛠️ Installation & Setup
1. **Clone the repository** and navigate to the root folder.
2. **Install dependencies**:
   ```bash
   pip install -r requirements.txt